# recommender_system_for_books



#to create the docker image/build run this command

"docker build -t streamlit-app ."

#to run the docker image run this command
"docker run -p 8501:8501 streamlit-app" 